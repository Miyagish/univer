#pragma once
#include "pch.h"

using namespace std;

int insertObj(vector <string> &);
int deleteObj(vector <string> &);
int editObj(vector <string> &);
int parseInput(vector <string>&, string&);
int printVector(vector <string>&, int);
int callMenu(vector <string>&);
